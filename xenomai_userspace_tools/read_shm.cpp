#include <xen_ringbuf_controller.h>


/* The name of this program.  */


/* Main program entry point.  ARGC contains number of argument list
 elements; ARGV is an array of pointers to them.  */
int main(int argc, char *argv[])
{

	/* A string listing valid short options letters.  */




	/*printf("p=%u (%llu) l=%u m=%u (%llu)\n", page_view_mode, page_to_view,
			last_written_data_mode, max_length_mode, maxlength);
	*/

	/*	from = page_index * maxlength;
		to = from + maxlength;
		if(from >= ringBuffer->size )
		{
			from = ringBuffer->size - maxlength;
			to = ringBuffer->size;
		}
		if(to >= ringBuffer->size )
		{
			to = ringBuffer->size;
		}*/
		//printf("from = %llu to = %llu\n", from, to);
		//read_shm(from, to, ringBuffer);


	//ringBuffer->index
	//ringBuffer->size / ringBuffer



}

